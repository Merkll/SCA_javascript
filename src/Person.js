class Person {
  constructor({ firstName, lastName, location, id }) {
    this.firstName = firstName;
    this.lastName = lastName;
    this.location = location;
    this.id = id;
  }
}

export default Person;
